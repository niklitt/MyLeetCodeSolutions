package org.litt.namesScoresPU;

/**
 * Approach:
 * First read in file
 * Parse input to get each name and add to List
 * Learn ASCII values (decide how to approach values of each letter, they are all in caps) ****
 * Use Collections.sort() or create another sorting method for fun
 * Put each name into a char array and calculate (value * position) add that to total sum
 * Return sum
 */
public class Solution {
}
